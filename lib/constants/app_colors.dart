import 'package:flutter/material.dart';

class Pallete {
  static const backgroundColor = Color(0xFFeceff6);

  static const khint = Color(0xFFD8DFE2);

  static const primaryColor = Color(0xFF0260ff);
  static const primary2=Color(0xFF0260ff);
  static const black = Color.fromARGB(255, 0, 0, 0);
  static const shadowcolor = Color(0xFFeeefef);
  static const whiteColor = Color(0xFFffffff);
  static const text = Color(0xFF466371);
  static const redColor = Color(0xFFFF3737);
  static const List<Color> gradientchart = [primaryColor, primaryColor];
  static const sGradient = LinearGradient(
      begin: Alignment.centerLeft,
      end: Alignment.centerRight,
      colors: [primaryColor, primaryColor]);
}
